@if (this.User.IsInRole("Administrator") || this.User.IsInRole("Moderator"))
{
    <ul class="nav navbar-nav">
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Administration<span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
                @if (this.User.IsInRole("Administrator"))
                {
                    <li>@Html.ActionLink("User Management", "index", "home")</li>
                }
                <li>@Html.ActionLink("Product Management", "index", "home")</li>
                <li>@Html.ActionLink("Order Management", "index", "home")</li>
                <li>@Html.ActionLink("Contact Management", "index", "home")</li>
            </ul>
        </li>
    </ul>
}
